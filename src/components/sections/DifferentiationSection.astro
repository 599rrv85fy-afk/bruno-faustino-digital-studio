<section class="differentiation">
  <div class="container">
    <div class="differentiation__grid">
      <div class="differentiation__intro">
        <p class="differentiation__eyebrow">
          Diferenciação
        </p>

        <h2 class="differentiation__title">
          Estratégia. Tecnologia. Resultados.
        </h2>
      </div>

      <div class="differentiation__content">
        <p class="differentiation__lead">
          Um website não deve existir apenas para parecer bem.
          Deve apoiar objetivos reais de negócio.
        </p>

        <p>
          Cada projeto é pensado a partir da estratégia, da experiência
          do utilizador, da performance, da visibilidade e da capacidade
          de evolução futura.
        </p>

        <p>
          O objetivo é construir soluções digitais mais claras,
          eficientes e preparadas para gerar valor ao longo do tempo.
        </p>
      </div>
    </div>

    <div class="differentiation__pillars">
      <article class="pillar">
        <span class="pillar__number">01</span>

        <h3>Estratégia</h3>

        <p>
          Compreender o negócio, identificar prioridades e definir
          uma direção antes de começar a construir.
        </p>
      </article>

      <article class="pillar">
        <span class="pillar__number">02</span>

        <h3>Tecnologia</h3>

        <p>
          Escolher ferramentas e soluções adequadas ao problema,
          evitando complexidade sem benefício real.
        </p>
      </article>

      <article class="pillar">
        <span class="pillar__number">03</span>

        <h3>Resultados</h3>

        <p>
          Criar experiências digitais orientadas para confiança,
          eficiência, conversão e crescimento sustentável.
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .differentiation {
    position: relative;

    overflow: hidden;

    background: var(--color-bg-primary);
  }

  .differentiation::before {
    content: "";

    position: absolute;
    top: -180px;
    right: -180px;

    width: 460px;
    height: 460px;

    border-radius: 50%;

    background:
      radial-gradient(
        circle,
        rgba(111, 130, 157, 0.16),
        rgba(111, 130, 157, 0)
      );

    pointer-events: none;
  }

  .differentiation__grid {
    position: relative;
    z-index: 1;

    display: grid;
    grid-template-columns:
      minmax(0, 1.1fr)
      minmax(320px, 0.9fr);

    align-items: start;

    gap: clamp(64px, 10vw, 140px);

    margin-bottom: var(--space-10);
  }

  .differentiation__eyebrow {
    margin-bottom: var(--space-6);

    color: var(--color-primary);

    font-size: 0.75rem;
    font-weight: 600;

    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .differentiation__title {
    max-width: 850px;

    margin-bottom: 0;
  }

  .differentiation__content {
    max-width: 620px;
  }

  .differentiation__content p {
    margin-bottom: var(--space-5);

    color: var(--color-text-secondary);
  }

  .differentiation__lead {
    color: var(--color-text-primary) !important;

    font-size: clamp(1.25rem, 2vw, 1.6rem);
    line-height: 1.5;
  }

  .differentiation__pillars {
    position: relative;
    z-index: 1;

    display: grid;
    grid-template-columns: repeat(3, 1fr);

    border-top: 1px solid var(--color-border);
  }

  .pillar {
    padding:
      var(--space-7)
      var(--space-6)
      0;

    border-right: 1px solid var(--color-border);
  }

  .pillar:first-child {
    padding-left: 0;
  }

  .pillar:last-child {
    padding-right: 0;

    border-right: 0;
  }

  .pillar__number {
    display: block;

    margin-bottom: var(--space-7);

    color: var(--color-text-muted);

    font-size: 0.75rem;
    font-weight: 600;

    letter-spacing: 0.12em;
  }

  .pillar h3 {
    margin-bottom: var(--space-4);

    font-size: clamp(1.4rem, 2vw, 2rem);
    font-weight: 500;
  }

  .pillar p {
    margin-bottom: 0;

    color: var(--color-text-secondary);

    line-height: 1.7;
  }

  @media (max-width: 900px) {
    .differentiation__grid {
      grid-template-columns: 1fr;

      gap: var(--space-7);

      margin-bottom: var(--space-9);
    }

    .differentiation__pillars {
      grid-template-columns: 1fr;
    }

    .pillar {
      padding: var(--space-6) 0;

      border-right: 0;
      border-bottom: 1px solid var(--color-border);
    }

    .pillar:first-child {
      padding-top: var(--space-6);
    }

    .pillar:last-child {
      border-bottom: 0;
    }

    .pillar__number {
      margin-bottom: var(--space-4);
    }
  }

  @media (max-width: 768px) {
    .differentiation__title {
      font-size: clamp(2.2rem, 10vw, 3.4rem);
      text-wrap: balance;
    }
  }
</style>